# WhatsApp финансовый бот

Читает сообщения из WhatsApp-группы, парсит суммы по людям/картам/наличным
и по команде `/отчет` генерирует PDF со сводкой.

## Статус кода

- **`internal/parser`** — полностью написан и **протестирован юнит-тестами**
  на твоих реальных сообщениях (5 тестов, все проходят). Это самая важная
  и рискованная часть логики, поэтому именно её я проверил в первую очередь.
- **`internal/db`, `internal/report`, `internal/bot`, `cmd/bot`** — написаны
  по документированным API библиотек (pgx, gofpdf, whatsmeow), но **не собраны
  в этой песочнице** — сеть здесь разрешает не все домены, нужные Go-модулям
  (`golang.org`, `gopkg.in` заблокированы). На твоём сервере с обычным интернетом
  `go mod tidy` подтянет всё без проблем.

**Что стоит сделать перед первым продакшн-запуском:** прогнать `go build ./...`
и `go vet ./...` на своей машине — если API какой-то библиотеки чуть разошёлся
с версией, зафиксированной в `go.mod`, компилятор сразу укажет на конкретную
строку, это будет быстрая правка.

## Структура проекта

```
whatsapp-bot/
├── cmd/bot/main.go           # точка входа
├── internal/
│   ├── parser/                # разбор текста сообщений (протестировано)
│   │   ├── parser.go
│   │   ├── aliases.go         # словарь синонимов имён (Пияна/Пиян и т.п.)
│   │   └── parser_test.go
│   ├── db/                    # PostgreSQL
│   │   ├── db.go
│   │   └── schema.sql
│   ├── report/pdf.go          # генерация PDF-отчёта
│   └── bot/bot.go             # интеграция с WhatsApp (whatsmeow)
├── assets/fonts/               # шрифты DejaVu для кириллицы в PDF
├── Dockerfile
├── docker-compose.yml
└── go.mod
```

## Установка на сервере

### 1. Клонирование и настройка

```bash
git clone <адрес твоего приватного репозитория> whatsapp-bot
cd whatsapp-bot
cp .env.example .env
nano .env   # заполни пароль Postgres; группы настраивать не обязательно — см. раздел "Группы" ниже
```

### 2. Запуск

```bash
docker compose up -d --build     # на старых серверах: docker-compose
docker compose logs -f bot
```

При первом запуске в логах появится **QR-код прямо в терминале** — отсканируй
его с телефона бота: **WhatsApp → Настройки → Связанные устройства →
Привязать устройство**. Если QR-картинка не читается с экрана, под ней
печатается текстовый код — его можно вставить в любой онлайн-генератор QR.

**Важно:** используй отдельный номер, не свой основной — это неофициальный
протокол, есть риск ограничений на аккаунте при подозрительной активности.

После сканирования сессия сохраняется в volume `bot_data` — при рестартах
контейнера повторный QR не нужен.

### 3. Группы

По умолчанию бот учитывает **все группы**, в которых состоит его номер —
ничего дополнительно настраивать не нужно, просто добавь номер бота в каждую
группу со сборами. Учёт по всем группам общий: суммы по одинаковым именам
складываются вместе независимо от того, в какой группе пришло сообщение;
команда `/отчет` работает в любой из этих групп.

Если нужно ограничить бота конкретными группами (например, номер добавлен
и в другие, нерабочие чаты) — впиши их JID через запятую в `.env`:

```
WHATSAPP_GROUP_JIDS=120363000000000001@g.us,120363000000000002@g.us
```

Чтобы узнать JID группы: напиши в неё любое сообщение и посмотри логи —
временно можно залогировать `msg.Info.Chat` для всех чатов, либо использовать
метод `client.GetJoinedGroups()`.

### 4. Обновление бота

```bash
cd whatsapp-bot
git pull
docker compose up -d --build
```

### 5. Команды в группе

- `/отчет`, `/отчёт`, `/report`, `/итог` — бот присылает PDF за текущий месяц
  прямо в группу
- **Обращение по имени**: напиши в группе «Джарвис скинь отчет за июль» или
  «Джарвис, кто сколько чеков скинул» — бот поймёт, что обращаются к нему,
  и ответит прямо в группе (отчёты и статистика; свободное общение и
  дозагрузка чеков — только в личке). Имя настраивается переменной
  `BOT_NAME` в `.env` (по умолчанию «Джарвис»). Сообщения, где имя не в
  начале или это часть слова («Джарвисом»), бот не трогает.

## Личный ассистент (через OpenRouter)

Если написать что-то в личку номеру бота (не в рабочую группу), бот отвечает
через модель Claude, подключённую через [OpenRouter](https://openrouter.ai)
(OpenAI-совместимый API). Ассистент:

- знает текущую сводку по сбору за месяц (по всем группам сразу) и может
  отвечать на вопросы вроде "сколько уже собрали с Ахмеда";
- умеет строить отчёт за **любой период по запросу** — например, "какой сбор
  был 3 и 4 июля", "скинь отчёт за прошлую неделю", "пришли пдф за июнь".
  Он сам переводит даты в нужный формат и решает, ответить цифрами прямо
  в переписке или прислать PDF-документом — в зависимости от того, как
  сформулирован запрос (если явно просишь "документ"/"пдф"/"отчёт" — пришлёт
  файл, если просто спрашиваешь сумму — ответит текстом);
- умеет фильтровать отчёты **по конкретной группе**: "скинь отчёт по группе
  оплата клиентов за июль" — бот знает названия всех своих групп;
- считает **сбор по сотрудникам**: каждый чек привязан к тому, кто его
  отправил. "Проверь, кто сколько чеков скинул в группе сб оплата клиентов
  за июль" или "сколько чеков и на какую сумму сделал Расул" — покажет
  по имени, а если имя в WhatsApp не сохранено — по номеру телефона;
- поддерживает **дозагрузку пропущенных чеков**: если бота добавили в группу
  с опозданием (например, 3 июля, а чеки шли с 1-го), пришли пропущенные
  чеки боту в личку и напиши "запомни их, это для группы оплата клиентов" —
  он распознает каждый, проверит на дубли и сохранит в учёт с **датой с самого
  чека** (включая год), так что они лягут на свои реальные дни;
- умеет принимать **массовый импорт истории**: на старом телефоне выдели
  чеки в чате (можно пачками) и перешли их на номер бота в личку. На каждое
  фото без подписи бот отвечает коротким подтверждением ("📥 Чек распознан:
  Милана, 29 400 ₽. Ожидают сохранения: 12") без лишней болтовни. Когда всё
  переслано — напиши "запомни их для группы такой-то", и вся пачка уйдёт в
  учёт с датами с самих чеков. Буфер вмещает до 300 чеков за раз;
- отвечает на вопросы **по конкретному человеку**: "сколько чеков у Ахмеда
  Каталова" — покажет число чеков, платежей, суммы и период операций.
  Опечатки в именах исправляет сам ("ахмет каталов" поймёт как "Ахмед");
- делает **PDF по запросу**: "скинь пдф кто сколько чеков скинул за июль" —
  пришлёт готовый документ с таблицей и итогом. Можно и произвольный:
  "сделай отчёт/чек с такими данными: ..." — бот оформит это в PDF-таблицу
  (цифры берёт только из учёта или из того, что ты явно назвал, — не выдумывает).

Личку бота можно закрыть от посторонних: пропиши в `.env` `OWNER_NUMBERS` —
номера через запятую (без плюса), которым разрешено писать боту напрямую.
Без этой переменной бот отвечает в личке всем.

## Кому доступна отчётность

Переменная `REPORT_ADMINS` (номера через запятую, 8... и 7... — оба формата
понимаются) задаёт, кто может спрашивать про сборы, суммы, отчёты и делать
корректировки. Всем остальным — и в личке, и в группе — бот на такие вопросы
вежливо отвечает «Извини, не могу с этим помочь — это конфиденциальная
информация» (суммы даже не попадают в контекст ИИ). При этом остальные
могут свободно общаться с ботом и проверить чек конкретного человека
("прошёл ли мой чек на 5000").

Бот игнорирует статусы (сторис) и каналы WhatsApp — на них он не отвечает
и ничего не публикует.

История переписки хранится в памяти процесса (не в БД) и сбрасывается при
перезапуске бота.

Чтобы включить: пропиши в `.env`:

```
OPENROUTER_API_KEY=sk-or-v1-твой_ключ
OPENROUTER_MODEL=~anthropic/claude-sonnet-latest   # можно не задавать — это значение по умолчанию
```

Ключ берётся на https://openrouter.ai/keys. Без `OPENROUTER_API_KEY` личные
сообщения просто игнорируются — вся остальная логика (учёт по группам,
отчёты, OCR) работает как обычно.

Чтобы включить: пропиши в `.env` / `docker-compose.yml`:

```yaml
ANTHROPIC_API_KEY: "твой_ключ_с_console.anthropic.com"
```

Без этой переменной личные сообщения просто игнорируются — вся остальная
логика (учёт по группе, отчёты, OCR) работает как обычно.

## OCR фото чеков

Бот умеет распознавать текст на фото чеков двумя способами (переключается
переменной `OCR_PROVIDER` в `docker-compose.yml`):

### Вариант 1 — Tesseract (бесплатно, по умолчанию)

Ничего покупать не нужно, движок уже встроен в Docker-образ. Точность
на кривых/тёмных фото заметно ниже, чем у облачных сервисов — используй
для старта, посмотри, устраивает ли качество распознавания.

### Вариант 2 — Yandex Vision (платно, точнее)

**Где купить/подключить:**

1. Зайди на **https://console.yandex.cloud**, зарегистрируйся (можно через аккаунт Яндекса)
2. В консоли создай **платёжный аккаунт** — привяжи карту. Новым пользователям
   обычно дают небольшой грант на пробу (сумма и условия меняются, смотри
   актуальные условия на сайте при регистрации)
3. Создай **каталог** (folder) — это рабочее пространство проекта
4. В разделе **IAM → Сервисные аккаунты** создай сервисный аккаунт,
   назначь роль **`ai.vision.user`**
5. Для этого сервисного аккаунта создай **API-ключ** (не путать с IAM-токеном —
   он не истекает, удобнее для сервера). Если консоль спросит область
   действия ключа — выбери **`yc.ai.foundationModels.execute`**
6. Скопируй значения:
   - `YANDEX_OCR_API_KEY` — сам ключ
   - `YANDEX_FOLDER_ID` — id каталога из шага 3

Бот ходит в актуальный эндпоинт OCR
(`https://ai.api.cloud.yandex.net/ocr/v1/recognizeText`, модель `page`,
языки распознаются автоматически).

**Стоимость:** тарификация Yandex Vision — за количество распознанных страниц/изображений,
цену на момент подключения смотри на странице тарифов Vision в Yandex Cloud
(она может меняться, поэтому точную цифру называть не буду — проверь при регистрации).
Для чеков одной кофейни в месяц расход обычно совсем небольшой — по несколько
рублей за десятки фото.

Чтобы включить, в `docker-compose.yml` раскомментируй три строки в `environment`
и пропиши свои значения:

```yaml
OCR_PROVIDER: "yandex"
YANDEX_OCR_API_KEY: "твой_api_ключ"
YANDEX_FOLDER_ID: "твой_folder_id"
```

## Непонятые чеки и пересчёт

Если бот не смог уверенно разобрать чек (незнакомое имя, нечитаемое фото,
нераспознанная сумма) — ничего не теряется. Спроси: «какие чеки ты не понял?»
— бот покажет список. «Скинь мне тот чек» — пришлёт сам файл (фото или PDF)
в чат. Посмотрел, продиктовал: «это чек Хадижат на 14000 от 2 июля» — бот
запишет его в учёт как обычный.

«Пересчитай» / «проанализируй всё заново» — полный пересчёт: бот повторно
сопоставляет непонятые чеки с известными именами, заново прогоняет
нераспознанные сообщения через парсер и ИИ, и все отчёты дальше строятся
по свежим данным.

## PDF-чеки

Банки часто отдают чеки PDF-файлами (Сбер «Документ-....pdf», ВТБ, РСХБ,
Т-Банк) — бот понимает их наравне с фото: скачивает, вытаскивает текстовый
слой (`pdftotext`), а если это скан без текста — рендерит первую страницу
в картинку и распознаёт через OCR. Фото, отправленные «как файл» (документом
без сжатия), тоже распознаются.

Распознавание идёт каскадом, чек проходит до победного:
1. текстовый слой PDF / OCR фото (Tesseract или Yandex Vision);
2. парсер полей по шаблонам банков;
3. если парсер не справился — ИИ доразбирает распознанный текст;
4. если и текст безнадёжен (нечитаемое фото) — **Claude смотрит на само
   изображение чека** и читает его «глазами»: банк, получатель, сумма,
   номер документа, дата со временем. Это работает и для фото, и для PDF.

Только если не справился даже вижн, чек попадает в «непонятые» — и его
можно разобрать вручную («какие чеки ты не понял?» → «скинь его» → диктуешь
данные). Дальше всё как с фото: парсинг полей,
сопоставление имени, дубли, учёт. Работает и в группах, и в личке
(в т.ч. при массовой пересылке истории).

## Автопересылка чеков между чатами

Говоришь ассистенту: «давай все чеки из группы оплата клиентов скидывай в
оплата клнт» — и бот начинает автоматически пересылать каждый чек (фото или
PDF), приходящий в группу-источник, в целевую группу, и сразу записывает его
в учёт целевой группы. Источником может быть и личка: «все чеки, что мне
кидают в личку, пересылай в оплата клнт». Выключается так же словами:
«хватит пересылать чеки» (все правила) или «останови пересылку из ...»
(одно правило). Правила хранятся в базе и переживают перезапуск бота;
текущие правила ассистент всегда знает и покажет по запросу.

## Понимание контекста, удалённые сообщения и корректировки

Бот отличает **реальные операции** от разговоров о деньгах: «Ахмед сказал
взять 5000» или «может нам надо 10000» в учёт не попадут — записываются
только фактически совершённые платежи («милана скинула полтора ляма на
сбер» → Милана, 1 500 000 ₽, сбер). Понимает сленг: 5к, 25 тыщ, лям,
полтора ляма, косарь. Если сообщение похоже на платёж, но непонятно
ключевое (например, «пришло 25 тыщ, кто это кинул?») — бот не запишет
наугад, а задаст уточняющий вопрос прямо в группе.

**Удалённые сообщения**: если сообщение с платежом или чеком удалили в
WhatsApp («Удалить у всех»), бот автоматически убирает связанные записи из
учёта и подтверждает это в чате. Отчёты всегда отражают актуальное состояние.

**Ручные корректировки**: скажи ассистенту (в личке или через обращение по
имени в группе) — «тот чек на 5000 скинули по ошибке, не считай его», «верни
платёж Миланы на 3000», или, если бот ошибочно счёл чек дублем — «это не дубль,
засчитай чек Миланы на 25000». Если под описание подходит несколько операций,
бот покажет список и уточнит, какую имели в виду.

**Удаление своих сообщений**: бот умеет отзывать собственные сообщения («удалить
у всех»). Скажи «удали своё последнее сообщение» (или «…в группе оплата клнт» —
из лички можно указать группу), и бот их уберёт. Чужие сообщения удалять он
не может — только свои.

## Дубли чеков

Если один и тот же чек прислали дважды **в одну и ту же группу** (случайно
переслали фото повторно), бот это замечает и предупреждает в чат: `⚠️ Похоже,
этот чек уже присылали в эту группу: Имя, 30 000 ₽, чек от 02.07.2026 14:12`.
Второй раз сумма **не учитывается** в отчётах (но сам чек сохраняется в базе
для истории).

Пересылка чека **между группами дублем не считается** — это рабочий процесс:
работники кидают чеки в группу СБ, оттуда их пересылают в основную группу.
Такой чек спокойно живёт в обеих группах, а в общем отчёте «по всем группам»
одинаковые операции автоматически схлопываются в одну, чтобы сумма не
задваивалась.

Дубль определяется по **совокупности параметров сразу**: получатель (ФИО с
чека), сумма, время операции и номер документа. Чек считается повтором, когда
совпали получатель, сумма и время, и при этом номера операции не противоречат
(совпадают либо у одного из чеков номер не распознан). Два разных перевода
одному человеку на одинаковую сумму дублем НЕ считаются — у них разные номера.
Чек, чьё сообщение удалили или который вручную исключили из учёта, перестаёт
считаться «оригиналом» — повторно прислать его можно.

Проверка идёт в два шага:
1. Если на чеке есть номер документа/код авторизации — сверяет их напрямую
   (это ID самой банковской операции, самый надёжный способ).
2. Иначе — сверяет получателя, сумму и **время самой операции с чека**
   (не время пересылки в WhatsApp) с окном ±5 минут.

Для этого бот теперь распознаёт дату и время прямо с фото чека (если банк
её печатает — так делают большинство: "26 июня 2026 22:58:35", "Дата
операции 27.06.2026 11:43:33" и т.п.). Это же время используется в отчётах:
если попросить у ассистента "скинь чеки за 2 июля", он покажет именно то,
что реально прошло по чекам 2 июля, а не то, что переслали в чат в этот день.

## Известные ограничения парсера (на что обратить внимание)

- Новые варианты написания имён (кроме уже прописанных Пияна/Пиян,
  Хадижат/Хадижа) нужно добавлять в `contact_aliases` в БД или в
  `internal/parser/aliases.go`
- Фото сохраняются в `data/receipts/` — можно периодически проверять глазами,
  особенно первые недели, пока не станет ясно, насколько хорошо OCR
  справляется именно с вашими чеками
- Если человек напишет сообщение в формате, которого нет в тестах, строка
  попадёт в лог "Не распознано" — стоит периодически проверять логи бота
  первые недели использования и дополнять парсер

## Как добавить новый алиас имени вручную

```sql
INSERT INTO contact_aliases (contact_id, alias)
SELECT id, 'новый_вариант_написания' FROM contacts WHERE canonical_name = 'Каноничное Имя';
```
